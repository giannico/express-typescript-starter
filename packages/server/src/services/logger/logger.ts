// instead of defining our own interface, just use winston's
// if we used a different logger in the future, this file
// would need to include our own custom interface
export { Logger } from 'winston';
